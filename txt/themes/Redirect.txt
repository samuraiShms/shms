Redirect

есть много способой как перередеректить человека на другую страницу


разберём три, которые я знаю

1. Navigate

return <Navigate to='/login' />
Импортируется из react-router-dom

2. useNavigate


Это хук
const nav = useNavigate()

а потом 
nav(и сюда вводим либо адрес страницы или можно также ввести число, например -1, что будет означать предыдущая страница)

3. redirect

Как по мне, он устарел, но вроде еще используется

return redirect('/login')